{
  "_comment": "Set the project ID with `firebase use --add` after cloning, then `firebase target:apply hosting angee-logo <site-id>`. The site-id should resolve to logo.angee.ai once the custom domain is added in Firebase console.",
  "projects": {},
  "targets": {}
}
